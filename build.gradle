plugins {
  id "org.sonarqube" version "2.5"
}

apply plugin: 'java'
apply plugin: 'maven'
apply plugin: 'application'

mainClassName = 'Calculadora.LCDTester'

repositories {
    jcenter()
}

uploadArchives {
    repositories.mavenDeployer {
            repository(url: "http://localhost:8888/repository/1235/"){
            	authentication (userName: 'Jorge', password: 'nacional95')
            }
            
            pom.version = '1.0'
            pom.artifactId = 'mylib'
            pom.groupId = 'com.duapps'
    }
}



dependencies {
    compile 'org.slf4j:slf4j-api:1.7.25'
    
    testCompile 'junit:junit:4.12'
}

